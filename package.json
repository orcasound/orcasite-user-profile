{
  "name": "orcasound-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "Simple static prototype server for OrcaHello1",
  "scripts": {
    "start": "node server.js",
    "dev": "live-server --port=5500 --no-browser --watch=./ --entry-file=index.html"
  },
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
